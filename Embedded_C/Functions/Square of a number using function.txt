#include <stdio.h>

int square(int num);

int main(void)
{
    int num, result;

    printf("Enter a number: ");
    scanf("%d", &num);

    result = square(num);

    printf("Square of %d = %d\n", num, result);

    return 0;
}

int square(int num)
{
    return num * num;
}