#include <stdio.h>

int main(void)
{
    int arr[5] = {10, 20, 30, 40, 50};
    int *ptr = arr;

    while (ptr < arr + 5)
    {
        printf("%d ", *ptr);
        ptr++;
    }

    printf("\n");

    return 0;
}