#include <stdio.h>

int main(void)
{
    int a = 5;
    int *ptr = &a;

    printf("Value of a = %d\n", a);
    printf("Address of a = %p\n", (void *)&a);
    printf("Value stored in ptr = %p\n", (void *)ptr);
    printf("Value using *ptr = %d\n", *ptr);

    return 0;
}