#include <stdio.h>

int main(void)
{
    int a;

    printf("Enter a value: ");
    scanf("%d", &a);

    int *p = &a;
    int **pp = &p;

    printf("Value of a = %d\n", a);
    printf("Value using *p = %d\n", *p);
    printf("Value using **pp = %d\n", **pp);

    return 0;
}